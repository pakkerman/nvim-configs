### This is a personal nvim configs

#### Usage:

Symlink into local config dir with stow, this is for (tested for mac for now, default config path may vary)
```
$ mkdir ~/.config/nvim
$ stow -t ~/.config/nvim {enter dir for the config}
```

